# 3e-docs_linear_tools

Work in `worktrees/expand-linear-graphql-api`.

## Objective

- Add `docs/LINEAR_TOOLS.md`: tool catalog, write vs read, unsupported GraphQL ops (no stubs).
- Update `packages/tools-linear/README.md` and `AGENTS.md` with module layout and tool list.

## Verify

Read-only; ensure markdown accurate vs `LINEAR_HUB_TOOLS` keys in `hub-tools.ts`.

## Commit

`docs: Linear tools surface and unsupported operations matrix`

# @workbench/sidecar

Interchange sidecar process. Connects to the hub via WebSocket, provisions agent sessions, and runs inference. Not modified directly — behavior is driven by agent definitions in `packages/agents`.

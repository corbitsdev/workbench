# @workbench/artifact

UI components for displaying and managing artifacts — the first-class output of GTM workflows (emails, one-pagers, follow-ups, etc.).

## Exports

- `ArtifactCard` — card view of a single artifact
- `ArtifactGallery` — grid of artifact cards

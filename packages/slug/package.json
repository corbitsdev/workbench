{
  "name": "@corbits/slug",
  "version": "0.0.1",
  "private": true,
  "description": "Human-readable URL slugs for named entities: slugify a display name, validate a slug read off a URL",
  "license": "LGPL-2.1-only",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}

# @workbench/tools-slack

Slack Web API tools for workbench agents: `slack_list_channels`,
`slack_get_channel_history`, `slack_search` (read) and `slack_post_message`
(write, approval-gated). The `slack` credential is a tenant-owned bot token
resolved by Interchange at tool execution time.

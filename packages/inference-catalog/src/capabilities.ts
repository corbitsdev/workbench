// Re-exported verbatim so this package has one import point without
// declaring a second vocabulary that could drift from the platform's.
export { Capability, WIRE_CAPABILITIES } from "@intx/types";

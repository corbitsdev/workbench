# @workbench/ui

UI foundation: design tokens (Tailwind v4 `@theme`), base components, and layout utilities. All other `@workbench/*` packages build on this.

Motion presets and `AnimatedNumber` are documented in [MOTION.md](./MOTION.md).

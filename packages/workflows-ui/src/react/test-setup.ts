/**
 * DOM environment for @workbench/workflows-ui React component tests.
 * Loaded via `bun test --preload ./src/react/test-setup.ts`.
 */
import { GlobalRegistrator } from "@happy-dom/global-registrator";

GlobalRegistrator.register();

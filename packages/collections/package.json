{
  "name": "@corbits/collections",
  "version": "0.0.1",
  "private": true,
  "description": "Bounded, self-evicting in-memory collections for process-lifetime state — a TTL map today, with orchestrator/crypto-cache callers () expected to consolidate onto it rather than growing their own",
  "license": "LGPL-2.1-only",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}

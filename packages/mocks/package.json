{
  "name": "@corbits/mocks",
  "version": "0.0.1",
  "private": true,
  "description": "Deterministic mocks of the inference boundary — local providers first — for tests that need a scriptable model and a record of exactly what was sent",
  "license": "LGPL-2.1-only",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./ollama": "./src/ollama/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "arktype": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}

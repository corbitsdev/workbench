{
  "buildCommand": "git submodule update --init --recursive && bun install && bun run --filter @gtm/web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "bun install",
  "framework": "vite"
}

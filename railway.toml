[build]
builder = "nixpacks"

[deploy]
startCommand = "bun run --filter @gtm/api start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

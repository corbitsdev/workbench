{
  "version": "0.7.14",
  "name": "gtm-workbench",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "workflows/*",
    "interchange/packages/*",
    "interchange/apps/admin-ui"
  ],
  "scripts": {
    "db:setup": "BUN_OPTIONS=--conditions=intx-src bun --env-file=.env run scripts/db-setup.ts",
    "db:seed": "BUN_OPTIONS=--conditions=intx-src bun --env-file=.env run scripts/db-seed.ts",
    "db:reset": "BUN_OPTIONS=--conditions=intx-src bun --env-file=.env run scripts/db-reset.ts",
    "dev": "BUN_OPTIONS=--conditions=intx-src bun --env-file=.env.local run --parallel --filter @workbench/web --filter @workbench/hub --filter @workbench/sidecar dev",
    "build": "BUN_OPTIONS=--conditions=intx-src bun run --parallel --filter @workbench/web --filter @workbench/hub build",
    "typecheck": "BUN_OPTIONS=--conditions=intx-src bun run --filter '@workbench/*' typecheck",
    "format": "BUN_OPTIONS=--conditions=intx-src bun run scripts/format-changed.ts",
    "format:all": "prettier --write .",
    "lint": "prettier --check . && eslint . && bun run lint:no-effect-fetch",
    "lint:no-effect-fetch": "BUN_OPTIONS=--conditions=intx-src bun run scripts/lint-no-effect-fetch.ts",
    "test": "BUN_OPTIONS=--conditions=intx-src bun run --filter '@workbench/*' test",
    "test:coverage": "BUN_OPTIONS=--conditions=intx-src bun run --filter '@workbench/*' test:coverage",
    "coverage:report": "COVERAGE_THRESHOLD=80 bun run scripts/coverage-merge.ts",
    "coverage": "BUN_OPTIONS=--conditions=intx-src bun run test:coverage && bun run coverage:report",
    "interchange:check": "BUN_OPTIONS=--conditions=intx-src bun run scripts/check-interchange-sync.ts"
  },
  "dependencies": {
    "arktype": "catalog:",
    "better-auth": "^1.6.9",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.10.7",
    "isomorphic-git": "^1.35.1",
    "postgres": "^3.4.9",
    "resend": "^4.0.0"
  },
  "overrides": {
    "@types/bun": "1.3.9",
    "bun-types": "1.3.9",
    "@intx/inference": "workspace:@workbench/inference"
  },
  "devDependencies": {
    "@eslint/js": "9.34.0",
    "eslint": "9.34.0",
    "prettier": "3.6.2",
    "typescript-eslint": "8.42.0",
    "typescript-language-server": "^5.3.0"
  },
  "engines": {
    "bun": ">=1.2.0"
  },
  "catalog": {
    "@types/semver": "^7.7.1",
    "arktype": "^2.2.0",
    "better-auth": "^1.4.18",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.10.7",
    "isomorphic-git": "^1.27.2",
    "semver": "^7.7.2",
    "ssri": "^12.0.0",
    "tar": "^7.5.1",
    "typescript": "^5.9.0"
  }
}
